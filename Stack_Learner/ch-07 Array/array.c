#include <stdio.h>

int main(){

    int array[5];

    array[0] = 25;
    array[1] = 30;
    array[2] = 30;
    array[3] = 50;
    array[4] = 70;

    // int index = 4;

    // printf("%d\n", array[0]+array[1]);

    // printf("%d\n", array[1*1+2*1]);
    // printf("%d", array[index]);

    int sum = 0;

    for(int i=0; i<= array[i];i++){
        sum += array[i];

    }
    printf("%d\n",sum);
    for(int j=0; j<=array[j];j++){
      

        if(array[j]== 50){
        printf("Identify Num: %d\n",array[j]);
        break;
    }
    else{
        printf("Sorry, No Number \n");
    }
    }
    

    return 0;
}