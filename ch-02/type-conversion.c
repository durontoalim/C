#include <stdio.h>
#include <math.h>

int main(void){

    // char A = 'M';
    // printf("%c %d %x\n ", A, A,A);

    // int fun = (int)A + 20;

    // printf("fun : %d", fun);

    // float flt = (float)fun;

    // printf("%f", flt);


    float a = 5.5;
    float b = 5.5;
    float sum = a+b;
    printf("%f\n", sum);

    float sum2= (int)a+ (int)b;
    printf("%f", sum2);

    return 0;
}